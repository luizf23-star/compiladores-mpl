name: Prova Entrega 1

on:
  push:
    branches: [main]

jobs:
  prova:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Rodar prova em clone limpo
        run: make prova E=1
